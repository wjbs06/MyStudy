
public class day03 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		 * 20180208

sysdm.cpl 을입력 엔터하면
시스템 속성창 띄움

고급탭 환경변수
JAVA_HOME=C:\Program Files\Java\jdk1.8.0_162
을 새로만들고
path=%JAVA_HOME%\bin;
편집에서 제일 앞에 붙여넣기

커맨드 창을 새로열면 path 설정됨.


	// 메모리 사이즈가 아니라 표현범위에 따른
	// 자동 형변환
		// byte -> short -> char -> int -> long -> float -> double
	// 강제 형변환(손실감수)
		// (자료형)상수 또는 변수
	// 숫자
		// 정수
			// byte < short < int < long
		// 실수
			// float < double

	// 문자
		// char
		
	// 불린
		// boolean

		// 연산자
		// 증감연산자 : ++,--
		// 산술연사자 : +,-,*,/,%
		// 비교연산자 : >,>=,==,!=,<=,<
		// 논리연산자 : &&,||, !

주석은 파일용량에 영향을 주지 않는다.


//제어문
		//조건문
			//if문(만약 ~이라면)
			//if(조건-t/f){실행문;}
			//if(조건-t/f){실행문;} else {실행문;}
			//*if(조건-t/f){
				실행문;
				}else if(조건){
				실행문;
				}else if(조건){
				실행문;
				}else if(조건){
				실행문;	
				...
				}else if(조건){
				실행문;
				}
	
	
	switch문
	switch(t/f가 들어와서는 안된다.){
		
		case 값:   자바에서는 값이 정수만 가능하다.
			실행문;
		break; 절대 빼면 안된다.
		case 값:
			실행문;
		break;
		case 값:
			실행문;
		break;
		case 값:
			실행문;
		break;
		default:
			실행문;
		break;
	}


	//제어문 - 반복문
	// for문
	for(1.초기화;2.조건식;3.증감){
		4.명령문;
	}
	1->2(true)->4->3->2(false)->out

	외부에서 선언된 변수는 내부에서 사용가능
	내부에서 선언된 변수는 외부에서 사용불가능

		 */

	}

}
